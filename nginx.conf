# Global settings
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log notice;
pid /run/nginx.pid;

# Load dynamic modules. Uncomment if necessary.
# include /usr/share/nginx/modules/*.conf;

# Event block
events {
    worker_connections 1024;
}

# HTTP block (contains server block)
http {
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Server block (make sure this is inside the http block)
    server {
        listen 80;
        server_name 10.251.158.29;  # Replace with your EC2 private IP or internal hostname

        location / {
            proxy_pass http://localhost:8080;  # Forward requests to Airflow web server running on localhost:8080
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
