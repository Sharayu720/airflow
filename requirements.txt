apache-airflow==2.6.1
psycopg2-binary  # If you're using PostgreSQL as the database backend
apache-airflow-providers-http  # If you're using HTTP operator
airflow[postgres]  # Install Airflow with PostgreSQL support
