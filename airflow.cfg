[core]
dags_folder = /home/ubuntu/airflow/dags/airflow
executor = LocalExecutor
# Additional configuration parameters
# (other settings for webserver, scheduler, etc.)

[webserver]
# Webserver specific settings
web_server_port = 8080
# (add more as needed)

[scheduler]
# Scheduler specific settings
scheduler_task_queued_timeout = 600
# (add more as needed)

[logging]
# Logging specific settings
base_log_folder = /home/ubuntu/airflow/logs
